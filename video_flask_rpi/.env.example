# Flask app secret for sessions
SECRET_KEY=change-this-secret

# Shared folder mounted on Raspberry (SMB/NFS mount)
MEDIA_ROOT=/mnt/media_share

# Shared desktop log file (same format/type as desktop app)
APP_LOG_PATH=./LOG/abrearch_premium.log

# Shared desktop SQLite DB (same file as desktop app)
DESKTOP_DB_PATH=./videos.db

# Auth credentials (change these)
APP_USERNAME=admin
APP_PASSWORD=change-this-password

# ffmpeg binary path in Raspberry
FFMPEG_PATH=/usr/bin/ffmpeg

# Server bind
HOST=0.0.0.0
PORT=8080
